#pragma once

#include <iostream>
#include <array>

//Header file with the declarations of the logic gates.

//Logic gate declaration
bool NAND(bool a, bool b); 

bool NOT(bool a);

bool AND(bool a, bool b);

bool XOR(bool a, bool b);

bool OR(bool a, bool b);

